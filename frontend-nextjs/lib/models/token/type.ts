export type TypeToken = {
    id: number
    token: string
    refresh_token: string
    access_token_invalidated: number
    refresh_token_invalidated: number
    ip_address: string
    user_agent: string
    created_at: string
}